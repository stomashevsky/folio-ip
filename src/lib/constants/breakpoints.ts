/** Mobile breakpoint in pixels — matches Tailwind `md:` (768px) and PlexUI --breakpoint-md. */
export const BREAKPOINT_MD = 768;


/** Large breakpoint in pixels — matches Tailwind `lg:` (1024px) and PlexUI --breakpoint-lg. */
export const BREAKPOINT_LG = 1024;

/** Extra-large breakpoint in pixels — matches Tailwind `xl:` (1280px) and PlexUI --breakpoint-xl. */
export const BREAKPOINT_XL = 1280;