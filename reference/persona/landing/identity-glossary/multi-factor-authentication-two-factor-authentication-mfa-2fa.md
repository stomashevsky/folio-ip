# Multi-Factor Authentication | Definition & FAQs

Multi-factor authentication (MFA) is an [authentication](./identity-authentication.md) method that combines two or more independent credentials: what the user knows (password), what the user has (security token), and who the user is (biometric) to authenticate the user's identity for login or another purpose.
